[build]
  command = "npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Allow CORS for ExerciseDB API images
[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
